# BT_NMS 常用部署指令

适用目录：`/Users/yangzijiang/BT_NMS`

执行前先进入工程目录：

```bash
cd /Users/yangzijiang/BT_NMS
```

说明：

- 现在各 compose 文件已经内置固定项目名
- `docker-compose.yml` -> `bt_nms`
- `docker-compose-prod.yml` -> `bt_nms_prod`
- `docker-compose-sy.yml` -> `bt_nms_sy`
- `docker-compose-sy-prod.yml` -> `bt_nms_sy_prod`
- 所以下面的命令默认都不再写 `-p`

## 备份项目（在项目文件夹路径执行，自动排除不需要备份的部分）
rsync -av --delete --exclude-from=BT_NMS/.rsync-filter BT_NMS/ BT_NMS_backup/

## 0. 部署 IP 配置

部署机器 IP 分别写在 BT 和 SY 自己目录下的文件中。

BT 配置文件：

- [backend/deploy_host_ip.txt](/Users/yangzijiang/BT_NMS/backend/deploy_host_ip.txt)

SY 配置文件：

- [sy_backend/deploy_host_ip.txt](/Users/yangzijiang/BT_NMS/sy_backend/deploy_host_ip.txt)

示例：

```text
192.168.1.88
```

也支持多个 IP，用逗号、分号或换行分隔：

```text
192.168.1.88
192.168.1.89
```

说明：

- BT 和 SY 分别读取自己的文件
- 会自动加入 `http://IP:8000`、`http://IP:8001`、`http://IP:38173`
- 改完这些文件后，重启对应 Django 容器进程即可生效
- 这一步通常不需要互联网，因为没有 `--build`

BT 开发环境：

```bash
docker restart bt_nms_django_app
```

BT 生产环境：

```bash
docker restart bt_nms_django_app0 bt_nms_nginx0
```

SY 开发环境：

```bash
docker restart bt_nms_sy_django_app
```

SY 生产环境：

```bash
docker restart bt_nms_sy_django_app0 bt_nms_sy_nginx0
```

推荐验证方法：

BT 生产环境：

```bash
docker exec bt_nms_django_app0 python manage.py shell -c "from django.conf import settings; print(settings.CSRF_TRUSTED_ORIGINS)"
```

SY 生产环境：

```bash
docker exec bt_nms_sy_django_app0 python manage.py shell -c "from django.conf import settings; print(settings.CSRF_TRUSTED_ORIGINS)"
```

## 0.1 告警延时小开关如何生效

BT 小开关文件：

- [backend/alarm_delay_switch_bt.py](/Users/yangzijiang/BT_NMS/backend/alarm_delay_switch_bt.py)

SY 小开关文件：

- [sy_backend/alarm_delay_switch_sy.py](/Users/yangzijiang/BT_NMS/sy_backend/alarm_delay_switch_sy.py)

说明：

- 改完小开关后，需要重启使用 `consts.py` 的 `summarize_alarms_container`
- 这一步不需要 `docker compose up --build`
- 直接 `docker restart` 即可

BT 开发环境：

```bash
docker restart bt_nms_summarize_alarms_container
```

BT 生产环境：

```bash
docker restart bt_nms_summarize_alarms_container0
```

SY 开发环境：

```bash
docker restart bt_nms_sy_summarize_alarms_container
```

SY 生产环境：

```bash
docker restart bt_nms_sy_summarize_alarms_container0
```

## 0.2 单独构建关键镜像
开发环境vue：
docker build -t my_vue:dev -f frontend/Dockerfile --build-arg VITE_BT_BACKEND_PORT=8000 --build-arg VITE_SY_BACKEND_PORT=8001 frontend

生产环境vue：
若部署机刚恢复互联网，先预拉取前端生产镜像依赖的基础镜像：

```bash
docker pull node:22
docker pull nginx:1.27-alpine-slim
```

docker build -t my_vue:prod -f frontend/Dockerfile.prod --build-arg VITE_BT_BACKEND_PORT=8000 --build-arg VITE_SY_BACKEND_PORT=8001 frontend

开发环境django：
docker build -t my_django:v5.0.6 -f backend/Dockerfile backend

生产环境dango：
docker build -t my_django:v5.0.6-prod -f backend/Dockerfile.prod backend
加个sy的tag：
docker tag my_django:v5.0.6-prod my_django:v5.0.6-sy-prod

## 0.3 Windows 宿主机运行 `bt_agent` / `sy_agent`

适用场景：

- `bt_agent` 和 `sy_agent` 不跑在 Docker 里
- 直接跑在 Windows 宿主机
- 直接使用宿主机已经安装好的 Python

相关脚本目录：

- [deploy/windows_host/README.md](/Users/yangzijiang/BT_NMS/deploy/windows_host/README.md)
- [deploy/windows_host/run_bt_agent.bat](/Users/yangzijiang/BT_NMS/deploy/windows_host/run_bt_agent.bat)
- [deploy/windows_host/run_sy_agent.bat](/Users/yangzijiang/BT_NMS/deploy/windows_host/run_sy_agent.bat)
- [deploy/windows_host/start_agents_on_boot.bat](/Users/yangzijiang/BT_NMS/deploy/windows_host/start_agents_on_boot.bat)
- [bt_agent/config.py](/Users/yangzijiang/BT_NMS/bt_agent/config.py)
- [sy_agent/config.py](/Users/yangzijiang/BT_NMS/sy_agent/config.py)

首次准备：

```bat
cd /d D:\BT_NMS\deploy\windows_host
```

说明：

- 两个 agent 现在优先读取各自目录下的 `config.py`
- 修改参数后直接重新启动对应 agent 即可生效
- 脚本优先使用 `py -3`，找不到时回退到 `python`

`bt_agent/config.py` 常改项：

```python
CONFIG = {
    "udp": {
        "host": "0.0.0.0",
        "port": 38315,
    },
    "redis": {
        "host": "127.0.0.1",
        "port": 36379,
    },
    "filters": {
        "blocked_ips": ["192.168.1.10", "192.168.1.11"],
    },
}
```

说明：

- `filters.blocked_ips` 是屏蔽 IP 列表
- 现在直接写在 Python 列表里
- 改完后重启 `bt_agent`

`sy_agent/config.py` 常改项：

```python
def make_devices(start_serial_id, end_serial_id, *, nms_offset=0, a1_interval=5.0):
    return [
        {
            "serial_id": serial_id,
            "nms_id": serial_id + nms_offset,
            "a1_interval": a1_interval,
        }
        for serial_id in range(start_serial_id, end_serial_id + 1)
    ]


CONFIG = {
    "redis": {
        "host": "127.0.0.1",
        "port": 36380,
        "db": 0,
    },
    "stream": {
        "cmd_consumer": "sy-agent-1",
    },
    "serial": {
        "default_baudrate": 19200,
        "timeout": 0.0,
    },
    "lines": [
        # 写法 1：逐一填写 devices
        {
            "line_id": 1,
            "name": "Line-1",
            "head_port": "COM3",
            "tail_port": "NONE",
            "baudrate": 19200,
            "timeout": 0.0,
            "devices": [
                {
                    "serial_id": 1,
                    "nms_id": 1,
                    "a1_interval": 5.0,
                },
                {
                    "serial_id": 2,
                    "nms_id": 2,
                    "a1_interval": 5.0,
                },
            ],
        },
        # 写法 2：用 make_devices 批量生成
        {
            "line_id": 2,
            "name": "Line-2",
            "head_port": "COM5",
            "tail_port": "NONE",
            "baudrate": 19200,
            "timeout": 0.0,
            "devices": make_devices(2, 4, nms_offset=2),
        },
        # 写法 3：同一个 line 里混合手写和批量生成
        {
            "line_id": 3,
            "name": "Line-3",
            "head_port": "COM7",
            "tail_port": "NONE",
            "baudrate": 19200,
            "timeout": 0.0,
            "devices": [
                {
                    "serial_id": 1,
                    "nms_id": 1,
                    "a1_interval": 5.0,
                },
                {
                    "serial_id": 2,
                    "nms_id": 2,
                    "a1_interval": 5.0,
                },
                *make_devices(10, 12, nms_offset=100),
            ],
        },
    ],
}
```

说明：

- 现场最常改的是 `redis.host`、`redis.port`、`lines` 里的串口号
- `make_devices(start, end, nms_offset=...)` 里的 `nms_offset` 用来生成 `nms_id`
- 计算规则是 `nms_id = serial_id + nms_offset`
- 例如 `make_devices(2, 4, nms_offset=2)` 会生成：
  - `serial_id=2 -> nms_id=4`
  - `serial_id=3 -> nms_id=5`
  - `serial_id=4 -> nms_id=6`
- 如果 `nms_offset=0`，那 `nms_id` 就和 `serial_id` 一样
- 同一个 `line` 的 `devices` 里可以同时放手写项和 `*make_devices(...)` 展开出来的项
- 混合写法时，不要让 `serial_id` 或 `nms_id` 重复
- `a1_interval` 表示这个设备多久插入一次 A1 轮询，单位是秒
- 平时轮询主要走 A2，到了 `a1_interval` 才会切一次 A1
- 如果同一条线上的设备很多，整条线轮询一圈花费的时间可能已经接近或超过 `a1_interval`
- 这时轮到某个设备时，条件又满足发 A1，可能导致这个设备长期更偏向发 A1，A2 次数明显变少
- 所以设备很多时，`a1_interval` 不要设太小，最好明显大于这条线扫完整圈的耗时
- 例如一条线扫完整圈大约要 20 秒，那么 `a1_interval` 更适合设成 30 到 60 秒，而不是 5 秒
- `no_resp_cmds`、`debug_tuning`、`cmd` 等参数一般按默认值即可
- 如果一条线不用，把对应 `head_port` 或 `tail_port` 写成 `"NONE"`

分别启动：

```bat
cd /d D:\BT_NMS\deploy\windows_host
run_bt_agent.bat
run_sy_agent.bat
```

同时启动：

```bat
cd /d D:\BT_NMS\deploy\windows_host
start_agents_on_boot.bat
```

开机启动：

```bat
cd /d D:\BT_NMS\deploy\windows_host
start_agents_on_boot.bat
```

推荐检查：

- 启动前确认 `bt_agent/config.py` 和 `sy_agent/config.py` 存在
- `run_bt_agent.bat`、`run_sy_agent.bat` 会先检查配置文件，不存在会直接报错退出
- `start_agents_on_boot.bat` 会打开两个可见窗口并显示实时输出，不写日志文件

## 1. 开发环境 `bt_nms`

首次部署或整体重建：

```bash
docker compose -f docker-compose.yml up -d --build --remove-orphans
```

只重启开发环境全部服务：

```bash
docker compose -f docker-compose.yml up -d --remove-orphans
```

只重启 BT 开发后端相关服务：

```bash
docker compose -f docker-compose.yml up -d --no-deps --force-recreate web udp_receiver summarize_alarms_container celery celery-beat
```

开发前端是 Vite 热更新，一般改完直接刷新：

```bash
docker logs --tail 50 bt_nms_vue_dev
```

## 2. BT 生产环境 `bt_nms_prod`

首次部署或整套更新：

```bash
docker compose -f docker-compose-prod.yml up -d --build --remove-orphans
```

只更新生产前端相关服务：

```bash
docker compose -f docker-compose-prod.yml up -d --remove-orphans vue
```

只更新 BT 生产后端相关服务：

```bash
docker compose -f docker-compose-prod.yml up -d --remove-orphans redis redis_stream db web nginx udp_receiver summarize_alarms_container celery celery-beat
```

只重建并更新 BT 生产 Django 镜像及后端服务：

```bash
docker compose -f docker-compose-prod.yml build web udp_receiver summarize_alarms_container celery celery-beat
docker compose -f docker-compose-prod.yml up -d --force-recreate web nginx udp_receiver summarize_alarms_container celery celery-beat
```

## 3. SY 开发环境 `bt_nms_sy`

首次部署或整套更新：

```bash
docker compose -f docker-compose-sy.yml up -d --build --remove-orphans
```

只更新 SY 开发后端相关服务：

```bash
docker compose -f docker-compose-sy.yml up -d --remove-orphans redis redis_stream db web sy_receiver summarize_alarms_container celery celery-beat
```

## 4. SY 生产环境 `bt_nms_sy_prod`

首次部署或整套更新：

```bash
docker compose -f docker-compose-sy-prod.yml up -d --build --remove-orphans
```

说明：

- `sy_backend/Dockerfile.prod` 现在是清华源优先，官方 PyPI 兜底
- 如果之前遇到 `zope.interface==6.4.post2` 这类“镜像源里找不到版本”的报错，直接重新 build 即可

只更新 SY 生产后端相关服务：

```bash
docker compose -f docker-compose-sy-prod.yml up -d --remove-orphans redis redis_stream db web nginx sy_receiver summarize_alarms_container celery celery-beat
```

只重建并更新 SY 生产 Django 镜像及后端服务：

```bash
docker compose -f docker-compose-sy-prod.yml build web sy_receiver summarize_alarms_container celery celery-beat
docker compose -f docker-compose-sy-prod.yml up -d --force-recreate web nginx sy_receiver summarize_alarms_container celery celery-beat
```

## 5. 生产环境前端修改如何应用

### BT 生产前端 `bt_nms_prod`

`docker-compose-prod.yml` 里的 `vue` 使用的是预构建镜像 `my_vue:prod`。

如果是现场电脑自己做少量前端源码修改，并且希望之后反复离线应用，推荐不要每次重建 `my_vue:prod` 镜像，而是改用“本地 `dist` 挂载”模式。

现场电脑少量前端修改的推荐流程：

1. 只在第一次有网时准备一次离线构建环境：

```bat
cd /d D:\BT_NMS\deploy\windows_host
prepare_frontend_offline_editing.bat
```

2. 之后现场电脑即使断网，也可以直接修改：

```text
D:\BT_NMS\frontend\src
```

3. 修改后离线重建并应用：

```bat
cd /d D:\BT_NMS\deploy\windows_host
build_and_apply_frontend_dist_offline.bat
```

说明：

- 这个脚本会离线执行 `npm ci --offline` 和 `npm run build`
- 然后用 `docker-compose-prod.frontend-local.yml` 挂载 `frontend/dist`
- 这样 `vue` 容器直接服务宿主机上的最新 `dist`
- 适合页面文案、小样式、小交互这类少量前端修改
- 如果改动较大，或者需要把结果带到别的机器，还是优先使用前面的 `docker save` / `docker load` 方案

推荐做法不是在现场离线 `docker build`，而是在有网机器先构建好最终镜像，再把镜像包带到离线现场。

推荐离线更新流程：

1. 在有网机器上重新构建前端生产镜像：

```bash
docker build -t my_vue:prod -f frontend/Dockerfile.prod --build-arg VITE_BT_BACKEND_PORT=8000 --build-arg VITE_SY_BACKEND_PORT=8001 frontend
```

2. 在有网机器上把最终镜像导出成 tar：

```bash
docker save -o my_vue_prod.tar my_vue:prod
```

Windows 宿主机也可以直接用脚本：

```bat
cd /d D:\BT_NMS\deploy\windows_host
build_and_export_frontend_prod_image.bat
```

3. 把 `my_vue_prod.tar` 拷贝到离线部署机。

4. 在离线部署机导入镜像：

```bash
docker load -i my_vue_prod.tar
```

Windows 宿主机也可以直接用脚本：

```bat
cd /d D:\BT_NMS\deploy\windows_host
import_and_update_frontend_prod_image.bat
```

5. 在离线部署机重建前端容器：

```bash
docker compose -f docker-compose-prod.yml up -d --no-deps --force-recreate vue
```

这样做的优点：

- 离线现场不需要访问 Docker Hub
- 离线现场不需要运行 `npm ci`
- 离线现场不需要准备 `node:22`、`nginx:1.27-alpine-slim`、`npm-cache`
- 现场更新动作简单，风险更低

如果现场机器也想自己离线构建前端镜像，再用下面这个备选方案。

备选方案 A：有网时先准备离线构建依赖

先拉基础镜像：

```bash
docker pull node:22
docker pull nginx:1.27-alpine-slim
```

再在 Linux `node:22` 容器里预热 npm 缓存：

```bash
docker run --rm \
  -v "$PWD/frontend:/app" \
  -w /app \
  node:22 \
  sh -lc 'rm -rf npm-cache && npm ci --cache ./npm-cache --no-audit --no-fund'
```

把下面这些内容一起带到离线环境：

- 整个项目代码目录
- 本地已有的 `node:22` 镜像
- 本地已有的 `nginx:1.27-alpine-slim` 镜像
- `frontend/npm-cache`

基础镜像可以先导出：

```bash
docker save -o node_22.tar node:22
docker save -o nginx_1.27-alpine-slim.tar nginx:1.27-alpine-slim
```

备选方案 B：在离线部署机本地 build

先导入基础镜像：

```bash
docker load -i node_22.tar
docker load -i nginx_1.27-alpine-slim.tar
```

再执行离线构建和重建：

```bash
docker build --network=none --pull=false -t my_vue:prod -f frontend/Dockerfile.prod --build-arg NPM_INSTALL_MODE=offline --build-arg VITE_BT_BACKEND_PORT=8000 --build-arg VITE_SY_BACKEND_PORT=8001 frontend

docker compose -f docker-compose-prod.yml up -d --no-deps --force-recreate vue
```

说明：

- `bt_nms_prod` 前端访问地址通常是 `http://localhost:38173/`
- 改完生产前端后，浏览器需要刷新；有缓存时请强刷
- 真正适合现场离线更新的主方案是 `docker save` / `docker load`，不是现场离线 `docker build`

### SY 生产前端

当前 `docker-compose-sy-prod.yml` 没有单独的 `vue` 服务。现在使用的是同一套 BT 生产前端入口，所以 SY 页面前端修改也按上面 `bt_nms_prod` 的前端重建方式处理。

## 6. 生产环境后端修改如何应用

### 只改了 Python / Django 模板 / admin 文案 / 业务代码

BT 生产环境：

```bash
docker compose -f docker-compose-prod.yml up -d --no-deps --force-recreate web nginx udp_receiver summarize_alarms_container celery celery-beat
```

SY 生产环境：

```bash
docker compose -f docker-compose-sy-prod.yml up -d --no-deps --force-recreate web nginx sy_receiver summarize_alarms_container celery celery-beat
```

说明：

- 当前生产 compose 里 `backend` / `sy_backend` 都是源码挂载到容器里的
- 所以只改 Python 代码时，通常不必先 `docker build`
- 但 Django 模板和 admin 页面这类修改，稳妥起见仍然要重建对应服务容器

### 改了 Dockerfile / Python 依赖 / 基础镜像

BT 生产环境：

```bash
docker compose -f docker-compose-prod.yml up -d --build --remove-orphans
```

SY 生产环境：

```bash
docker compose -f docker-compose-sy-prod.yml up -d --build --remove-orphans
```

## 7. 数据库迁移

BT 开发环境：

```bash
docker exec -it bt_nms_django_app python manage.py makemigrations
docker exec -it bt_nms_django_app python manage.py migrate
```

BT 生产环境：

```bash
docker exec -it bt_nms_django_app0 python manage.py migrate
```

SY 开发环境：

```bash
docker exec -it bt_nms_sy_django_app python manage.py makemigrations
docker exec -it bt_nms_sy_django_app python manage.py migrate
```

SY 生产环境：

```bash
docker exec -it bt_nms_sy_django_app0 python manage.py migrate
```

## 8. 常用查看与排查

查看容器：

```bash
docker ps
```

查看 BT 生产关键日志：

```bash
docker logs --tail 100 bt_nms_django_app0
docker logs --tail 100 bt_nms_udp_receiver0
docker logs --tail 100 bt_nms_summarize_alarms_container0
docker logs --tail 100 bt_nms_vue_prod0
```

查看 SY 生产关键日志：

```bash
docker logs --tail 100 bt_nms_sy_django_app0
docker logs --tail 100 bt_nms_sy_receiver0
docker logs --tail 100 bt_nms_sy_summarize_alarms_container0
```

持续跟日志：

```bash
docker logs -f bt_nms_udp_receiver0
docker logs -f bt_nms_sy_receiver0
```

清理旧孤儿容器：

```bash
docker compose -f docker-compose-prod.yml up -d --remove-orphans
docker compose -f docker-compose-sy-prod.yml up -d --remove-orphans
```

## 9. 常用访问地址

- BT 开发前端：`http://localhost:38173/`
- BT 开发后端：admin：`http://localhost:8000/admin/`
- SY 开发后端：admin：`http://localhost:8001/admin/`
- BT 生产前端：`http://localhost:38173/`
- BT 生产后端：admin：`http://localhost:8000/admin/`
- SY 生产后端：admin：`http://localhost:8001/admin/`
